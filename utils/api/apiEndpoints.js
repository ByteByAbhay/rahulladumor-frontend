class apiEndpoints {
  static base = '/api';

  //profile
  static profile = `${this.base}/profile`;
  
  //contact
  static contact = `${this.base}/contact`;

  //articles
  static articles = `${this.base}/articles`;
}

export default apiEndpoints;
 