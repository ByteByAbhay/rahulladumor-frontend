class apiEndpoints {
  public static base = '/api';

  //profile
  public static profile = `${this.base}/profile`;
  
  //contact
  public static contact = `${this.base}/contact`;

  //articles
  public static articles = `${this.base}/articles`;
}

export default apiEndpoints;
